#
# WARNING: To install Immich, follow our guide: https://docs.immich.app/install/docker-compose
#
# Make sure to use the docker-compose.yml of the current release:
#
# https://github.com/immich-app/immich/releases/latest/download/docker-compose.yml
#
# The compose file on main may not be compatible with the latest release.

name: immich
services:
  immich-server:
    container_name: immich_server
    image: ghcr.io/immich-app/immich-server:${IMMICH_VERSION:-release}
    devices:
      - /dev/dri:/dev/dri
    environment:
      - IMMICH_FFMPEG_ACCELERATION=quicksync
    # extends:
    #   file: hwaccel.transcoding.yml
    #   service: cpu # set to one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl] for accelerated transcoding
    volumes:
      # Do not edit the next line. If you want to change the media storage location on your system, edit the value of UPLOAD_LOCATION in the .env file
      #- ${UPLOAD_LOCATION}:/data
      - /etc/localtime:/etc/localtime:ro
      - /twins/photos/library:/data/library
      - /twins/photos/backups:/data/backups
      - /docker-cache/immich/thumbs:/data/thumbs
      - /docker-cache/immich/encoded-video:/data/encoded-video
      - /opt/immich/upload:/data/upload
      - /twins/photos/profile:/data/profile
    env_file:
      - .env
    ports:
      - 2283:2283
    depends_on:
      - redis
      - database
    restart: always
    healthcheck:
      disable: false

  immich-machine-learning:
    container_name: immich_machine_learning
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    # Example tag: ${IMMICH_VERSION:-release}-cuda
    image: ghcr.io/immich-app/immich-machine-learning:${IMMICH_VERSION:-release}-openvino
    # extends: # uncomment this section for hardware acceleration - see https://docs.immich.app/features/ml-hardware-acceleration
    #   file: hwaccel.ml.yml
    #   service: cpu # set to one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn] for accelerated inference - use the `-wsl` version for WSL2 where applicable
    devices:
      - /dev/dri:/dev/dri
    environment:
      - IMMICH_MACHINE_LEARNING_DEVICE=GPU
    volumes:
      - model-cache:/cache
    env_file:
      - .env
    restart: always
    healthcheck:
      disable: false

  redis:
    container_name: immich_redis
    image: docker.io/valkey/valkey:9@sha256:3eeb09785cd61ec8e3be35f8804c8892080f3ca21934d628abc24ee4ed1698f6
    healthcheck:
      test: redis-cli ping || exit 1
    restart: always

  database:
    container_name: immich_postgres
    image: ghcr.io/immich-app/postgres:14-vectorchord0.4.3-pgvectors0.2.0@sha256:bcf63357191b76a916ae5eb93464d65c07511da41e3bf7a8416db519b40b1c23
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_DB: ${DB_DATABASE_NAME}
      POSTGRES_INITDB_ARGS: --data-checksums
      # Uncomment the DB_STORAGE_TYPE: 'HDD' var if your database isn't stored on SSDs
      # DB_STORAGE_TYPE: 'HDD'
    volumes:
      # Do not edit the next line. If you want to change the database storage location on your system, edit the value of DB_DATA_LOCATION in the .env file
      - ${DB_DATA_LOCATION}:/var/lib/postgresql/data
    shm_size: 128mb
    restart: always
    healthcheck:
      disable: false

  immich-deduper:
    container_name: immich-deduper
    hostname: immich-deduper
    image: immich-deduper:v12 #${DEDUP_IMAGE:-razgrizhsu/immich-deduper:latest}
    restart: unless-stopped
    ports:
      - ${DEDUP_PORT:-8086}:${DEDUP_PORT:-8086}
    volumes:
      - ${DEDUP_DATA}:/app/data # deduper data
      - ${DEDUP_DATA}/cache:/root/.cache/torch/ # model cache
      - ${IMMICH_PATH}:/immich:ro # Read-only access to Immich photos
      # Optional: for separate thumbnail directory (uncomment if using IMMICH_THUMB)
      - ${IMMICH_THUMB}:/thumbs:ro
      - /docker-cache/immich/encoded-video:/immich/encoded-video:ro
    depends_on:
      - qdrant
    env_file:
      - .env

  qdrant:
    container_name: immich-deduper-qdrant
    image: qdrant/qdrant:v1.17.1
    restart: unless-stopped
    volumes:
      - ${DEDUP_DATA}/qdrant:/qdrant/storage
    env_file:
      - .env

volumes:
  model-cache: null

networks:
  default:
    external: true
    name: homenet
>>>>>>> origin/main
